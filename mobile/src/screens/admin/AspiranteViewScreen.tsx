import React from 'react';
import { View, Text, ScrollView, StyleSheet } from 'react-native';
import OfertasListScreen from '../aspirante/OfertasListScreen';
import { colors, spacing, fontSize, fontWeight, globalStyles } from '../../styles/theme';

const AspiranteViewScreen = () => {
  return <OfertasListScreen />;
};

export default AspiranteViewScreen;
