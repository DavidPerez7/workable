import React from 'react';
import { View, Text, ScrollView, StyleSheet } from 'react-native';
import MisOfertasListScreen from '../reclutador/MisOfertasListScreen';
import { colors, spacing, fontSize, fontWeight, globalStyles } from '../../styles/theme';

const ReclutadorViewScreen = () => {
  return <MisOfertasListScreen />;
};

export default ReclutadorViewScreen;
