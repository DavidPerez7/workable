info:
  name: Listar aspirantes
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/aspirante"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
