info:
  name: GetAdministradorById
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/administrador/1"
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
