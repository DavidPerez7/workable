info:
  name: DeleteMe
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/aspirante/me/delete"
  auth:
    type: bearer
    token: "{{aspiranteToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
