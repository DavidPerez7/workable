info:
  name: AsignarEmpresaByCode
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/api/reclutador/empresa/ESNFRHEFBGL3"
  auth:
    type: bearer
    token: "{{reclutadorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
