info:
  name: Delete
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/oferta/2"
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
