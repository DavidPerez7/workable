info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/municipio"
  body:
    type: json
    data: |-
      {
        "id": 21,
        "nombre": "Soacha",
        "departamento": "CUNDINAMARCA"
      }
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5