info:
  name: GetByOferta
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/postulacion/oferta/1"
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
