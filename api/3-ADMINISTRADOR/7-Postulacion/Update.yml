info:
  name: Update
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/api/postulacion/2"
  body:
    type: json
    data: |-
      {
        "estado": "ACEPTADO"
      }
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
