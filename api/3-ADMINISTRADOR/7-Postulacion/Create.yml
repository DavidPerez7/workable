info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/postulacion"
  body:
    type: json
    data: |-
      {
        "oferta": {
          "id": 1
        },
        "aspirante": {
          "id": 2
        }
      }
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
