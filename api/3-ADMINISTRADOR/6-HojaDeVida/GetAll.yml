info:
  name: GetAll
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/hoja-vida"
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
