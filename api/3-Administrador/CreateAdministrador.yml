info:
  name: CreateAdministrador
  http:
    method: POST
    url: "{{baseUrl}}/api/administrador"
    auth:
      type: bearer
      token: "{{adminToken}}"
    body:
      json: |
        {
          "nombre": "Super",
          "apellido": "Admin",
          "correo": "admin@example.com",
          "password": "admin123"
        }
