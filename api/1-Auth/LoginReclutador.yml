info:
  name: LoginReclutador
  http:
    method: POST
    url: "{{baseUrl}}/api/auth/login"
    body:
      json: |
        {
          "correo": "reclutador@example.com",
          "password": "pass123"
        }
