info:
  name: GetAllEmpresas
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/empresa"
  auth:
    type: bearer
    token: "{{reclutadorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
