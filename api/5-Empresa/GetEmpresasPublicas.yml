info:
  name: GetEmpresasPublicas
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/empresa/publicas"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
