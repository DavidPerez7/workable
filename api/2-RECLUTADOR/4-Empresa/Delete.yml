info:
  name: DeleteEmpresa
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/empresa/2"
  auth:
    type: bearer
    token: "{{reclutadorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
