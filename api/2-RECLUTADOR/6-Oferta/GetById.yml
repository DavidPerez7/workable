info:
  name: GetById
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/oferta/2"
  auth:
    type: bearer
    token: "{{reclutadorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
