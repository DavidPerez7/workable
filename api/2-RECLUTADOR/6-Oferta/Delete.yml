info:
  name: Delete
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/oferta/2"
  auth:
    type: bearer
    token: "{{reclutadorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
