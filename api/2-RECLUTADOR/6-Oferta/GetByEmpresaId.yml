info:
  name: GetByEmpresaId
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/oferta/empresa/1"
  auth:
    type: bearer
    token: "{{reclutadorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5