info:
  name: GetByOfertaId
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/postulacion/oferta/1"
  auth:
    type: bearer
    token: "{{reclutadorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
