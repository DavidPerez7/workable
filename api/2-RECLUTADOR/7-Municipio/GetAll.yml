info:
  name: GetAllMunicipios
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/municipio"
  auth:
    type: bearer
    token: "{{reclutadorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
