info:
  name: GetAspiranteById
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/aspirante/1"
  auth:
    type: bearer
    token: "{{reclutadorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
