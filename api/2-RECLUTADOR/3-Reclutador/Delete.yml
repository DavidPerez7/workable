info:
  name: Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/reclutador/2"
  auth:
    type: bearer
    token: "{{reclutadorToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
