info:
  name: GetHojaVidaByAspirante
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/hoja-vida/aspirante/2"
  auth:
    type: bearer
    token: "{{aspiranteToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
