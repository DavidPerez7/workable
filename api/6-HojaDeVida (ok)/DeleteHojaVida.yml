info:
  name: DeleteHojaVida
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/hoja-vida/2"
  auth:
    type: bearer
    token: "{{aspiranteToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
