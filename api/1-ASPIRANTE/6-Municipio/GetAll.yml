info:
  name: GetAll
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/municipio"
  auth:
    type: bearer
    token: "{{aspiranteToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
