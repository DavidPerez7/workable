info:
  name: GetByAspiranteId
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/postulacion/aspirante/1"
  auth:
    type: bearer
    token: "{{aspiranteToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5