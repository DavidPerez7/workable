import React from "react";

const ReclutadorLoadingState = ({ children = "Cargando..." }) => {
  return <div className="reclutador-loading-RP">{children}</div>;
};

export default ReclutadorLoadingState;
